\ProvidesPackage{macros}


%\documentclass[12pt]{article}

%Added stuff#1
%https://github.com/matlab2tikz/matlab2tikz#usage
\usepackage{pgfplots}
\pgfplotsset{compat=newest}
%% the following commands are needed for some matlab2tikz features
\usetikzlibrary{plotmarks}
\usetikzlibrary{arrows.meta}
\usepgfplotslibrary{patchplots}
\usepackage{grffile}
\usepackage{amsmath}

%% you may also want the following commands
%\pgfplotsset{plot coordinates/math parser=false}
%\newlength\figureheight
%\newlength\figurewidth

%https://tex.stackexchange.com/questions/173469/tikzmatlab2tikz
\usepackage{standalone}

%Added stuff#2
\usepackage{cancel}

%https://tex.stackexchange.com/questions/326375/boxed-border-color
% Definition of \boxed in amsmath.sty:
% \newcommand{\boxed}[1]{\fbox{\m@th$\displaystyle#1$}}

\usepackage{xcolor}

% Syntax: \colorboxed[<color model>]{<color specification>}{<math formula>}
\newcommand*{\colorboxed}{}
\def\colorboxed#1#{%
  \colorboxedAux{#1}%
}
\newcommand*{\colorboxedAux}[3]{%
  % #1: optional argument for color model
  % #2: color specification
  % #3: formula
  \begingroup
    \colorlet{cb@saved}{.}%
    \color#1{#2}%
    \boxed{%
      \color{cb@saved}%
      #3%
    }%
  \endgroup
}


%Added stuff#3 - pdf outline and blindtext
\usepackage[bookmarks=true]{hyperref}
\usepackage{bookmark}
\usepackage{blindtext}


%Added stuff#4 - tailored footer
\usepackage{fancyhdr}
\renewcommand{\headrulewidth}{0pt}
\fancyhf{} % clear existing header/footer entries
% We don't need to specify the O coordinate
\fancyfoot[L]{Nick }
\fancyfoot[C]{Page \thepage}
%\fancyfoot[R]{ST1PS}
\renewcommand{\footrulewidth}{0.4pt}


%Added stuff#5 - Nota bene ligature 
%https://tex.stackexchange.com/questions/483627/n-b-ligature-in-latex
\newcommand\NB[1][0.3]{N\kern-#1em\textcolor{red}{B}}
\usepackage{lettrine}

%Added stuff#6 - left tag 
%https://tex.stackexchange.com/questions/193535/switch-between-leqno-and-reqno-options-of-amsmath-in-the-same-document
\makeatletter
\newcommand{\leqnomode}{\tagsleft@true}
\newcommand{\reqnomode}{\tagsleft@false}
\makeatother


%Added stuff#7 - Multiline inside align environment
%https://tex.stackexchange.com/questions/29730/how-can-i-use-multline-within-an-align-environment
\usepackage{mathtools}
\usepackage[nopar]{lipsum} % for dummy text only

%Environment stuff

\usepackage[margin=1in]{geometry} 
\usepackage{amsmath,amsthm,amssymb}
\usepackage{enumitem}
\usepackage[dvipsnames]{xcolor}
% \usepackage[T2A]{fontenc} % Language support
\usepackage{listings}


\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\R}{\mathbb{R}}


\newenvironment{ex}[2][Exercise]{\begin{trivlist}
\item[\hskip \labelsep {\bfseries #1}\hskip \labelsep {\bfseries #2.}]}{\end{trivlist}}

%\newenvironment{sol}[1][\large Solution]{\begin{trivlist}
%\item[\hskip \labelsep {\bfseries #1:}]}{\end{trivlist}}

\newenvironment{sol}[1][Solution]{\begin{trivlist}
\item[\hskip \labelsep {\bfseries #1:}]}{\end{trivlist}}

\newenvironment{pr}[2][Problem]{\begin{trivlist}
\item[\hskip \labelsep {\bfseries #1}\hskip \labelsep {\bfseries #2.}]}{\end{trivlist}}

  %List environment
  %https://www.overleaf.com/learn/latex/Lists#Customizing_lists:_changing_the_layout
  \newcounter{counter}
  \newcommand{\makeboxlabel}[1]{\bfseries{(#1)}\hfill}% \hfill fills the label box
  \newenvironment{boxlabel}
    {\begin{list}
      {\alph{counter}}
      {\usecounter{counter}
       \setlength{\labelwidth}{2.4em}
       \setlength{\labelsep}{0em}
       \setlength{\itemsep}{2pt}
       \setlength{\leftmargin}{1.0cm}
       \setlength{\rightmargin}{0cm}
       \setlength{\itemindent}{0em} 
       \let\makelabel=\makeboxlabel
      }
    }
  {\end{list}}

%\newcommand{\Qstitle}[1]

\newcounter{counter2}
\newcommand{\makeboxlabelr}[1]{\bfseries{(#1)}\hfill}% \hfill fills the label box
\newenvironment{boxlabelr}
  {\begin{list}
    {\roman{counter2}}
    {\usecounter{counter2}
     \setlength{\labelwidth}{2.4em}
     \setlength{\labelsep}{0em}
     \setlength{\itemsep}{2pt}
     \setlength{\leftmargin}{1.0cm}
     \setlength{\rightmargin}{0cm}
     \setlength{\itemindent}{0em} 
     \let\makelabel=\makeboxlabelr
    }
  }
{\end{list}}


\renewcommand{\d}[1]{\ensuremath{\operatorname{d}\!{#1}}}

\usepackage{bm}
% better for typesetting
\newcommand{\vect}[1]{\mathbf{#1}} % or \bm
%\newcommand{\matr}[1]{\mathbf{#1}} % or \bm

% --------------------------------------------------------------
%                         Titles
% --------------------------------------------------------------


%\title{\textbf{ST1PS Practice Paper}}
%\author{Nick Scerbacenco}
%\date{ }


%\begin{document}
%\pagestyle{fancy}

% --------------------------------------------------------------
%                         Start here
% --------------------------------------------------------------


% -------------------------------------------------1